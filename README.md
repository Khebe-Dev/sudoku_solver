🧩 Sudoku Solver

A Python command-line program that reads a Sudoku puzzle from a file, solves it using backtracking, and writes the solution to an output file.

## Usage
python solve.py path/to/puzzle.txt -o path/to/solved_puzzle.txt

## Features

Validates rows, columns, and 3×3 subgrids

Uses backtracking to find a valid solution

Displays the solving process step by step

## Tech

Python
